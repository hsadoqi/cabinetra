# Build artifacts in src/ should not be committed
# These are generated by the TypeScript compiler during build
src/**/*.js
src/**/*.d.ts
src/**/*.d.ts.map

# Only the compiled dist/ directory should be committed if needed
# dist/ should typically be in root .gitignore instead
